% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/melsi_robust.R
\name{melsi}
\alias{melsi}
\title{Run MeLSI Analysis}
\usage{
melsi(
  X,
  y,
  analysis_type = "auto",
  n_perms = 200,
  B = 30,
  m_frac = 0.8,
  show_progress = TRUE,
  plot_vip = TRUE,
  correction_method = "BH",
  BPPARAM = NULL,
  seed = NULL
)
}
\arguments{
\item{X}{A matrix of feature abundances with samples as rows and features as columns}

\item{y}{A vector of group labels for each sample}

\item{analysis_type}{Type of analysis to perform:
- "auto" (default): Automatically choose based on number of groups
- "pairwise": For 2 groups or all pairwise comparisons for 3+ groups
- "omnibus": Global analysis for 3+ groups (requires at least 3 groups)
- "both": Both omnibus and pairwise for 3+ groups}

\item{n_perms}{Number of permutations for p-value calculation (default: 200)}

\item{B}{Number of weak learners in the ensemble (default: 30)}

\item{m_frac}{Fraction of features to use in each weak learner (default: 0.8)}

\item{show_progress}{Whether to display progress information (default: TRUE)}

\item{plot_vip}{Whether to display Variable Importance Plot (default: TRUE)}

\item{correction_method}{Multiple testing correction method for pairwise comparisons (default: "BH")}

\item{BPPARAM}{A \code{\link[BiocParallel]{BiocParallelParam}} object specifying the
parallel backend to use for permutation testing. If \code{NULL} (default),
permutations run sequentially. Requires the \pkg{BiocParallel} package.}

\item{seed}{Optional integer used to set the random seed for reproducible
results. If \code{NULL} (default), the current RNG state is used unchanged.}
}
\value{
An object of class \code{"melsi"} (a list, so existing \code{$}
        access is unchanged). For 2 groups or pairwise analysis it holds the
        F-statistic, p-value, and feature weights; for 3+ groups it contains
        the omnibus results, pairwise results, or both.
}
\description{
Performs MeLSI (Metric Learning for Statistical Inference) analysis for microbiome data.
Automatically handles both pairwise comparisons (2 groups) and multi-group analysis (3+ groups).
}
\examples{
# Generate test data
test_data <- generate_test_data(n_samples = 40, n_taxa = 50, n_signal_taxa = 5)
X <- test_data$counts
y <- test_data$metadata$Group

# CLR transformation
X_clr <- clr_transform(X)

# Run MeLSI analysis
results <- melsi(X_clr, y, n_perms = 19, B = 10, show_progress = FALSE)

# Check results
stopifnot(is.list(results))
stopifnot("F_observed" \%in\% names(results))
stopifnot("p_value" \%in\% names(results))

}
